services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=attuschallengedatabase'
      - 'MYSQL_PASSWORD=secret'
      - 'MYSQL_ROOT_PASSWORD=admin'
      - 'MYSQL_USER=admin'
    ports:
      - '3306'
